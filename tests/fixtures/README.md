# Test Fixtures

`tests/fixtures` is reserved for shared Minecraft-specific test fixtures.

Use this directory only when a fixture must be shared across modules. Keep
module-local tests and fixtures beside the module they verify whenever possible.

