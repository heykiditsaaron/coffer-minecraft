package dev.coffer.adapter.fabric.boundary;

/**
 * FABRIC ADAPTER — DECLARED METADATA
 *
 * Placeholder for future metadata snapshot.
 * Responsibility is limited to being an opaque container.
 */
public final class DeclaredMetadata {
    // Intentionally empty until metadata snapshots are declared explicitly.
}
