package dev.coffer.adapter.fabric.boundary;

/**
 * FABRIC ADAPTER — METADATA RELEVANCE DECLARATION
 *
 * Responsibility:
 * - Make the adapter's metadata stance explicit.
 */
public enum MetadataRelevance {
    RELEVANT,
    IGNORED_BY_DECLARATION,
    UNDECLARED
}
